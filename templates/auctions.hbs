<h4 class="text-center section-header">All Auctions</h4>

{{#link-to 'new-auction'}}Add Auction{{/link-to}}

<div class="items-wrapper">
  {{#each auction in model}}
    {{#link-to 'auction' auction}}
      <p class="title">{{auction.name}}</p>
    {{/link-to}}
  {{/each}}
</div>
